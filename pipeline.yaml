apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: samplepl
spec:
  params:
    - name: image-scan

    
  workspaces:
  - name: workspace

  tasks:
      
  - name: scan-image
    
    taskRef:
      name: scan-image
    workspaces: 
      - name: source
        workspace: workspace
    params:
      - name: image-scan
        value: $(params.image-scan)
    


  
  
   
      
