apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: sampleplrun
spec:
  pipelineRef:
    name: samplepl
  
  workspaces:
  - name: workspace
    persistentvolumeclaim:
      claimName: trivypvc

  params:
  
  - name: image-scan
    value: vulnerables/web-dvwa:1.9  
  
